namespace DuckDBGeoparquet.Models
{
    /// <summary>
    /// Shared constants used across the add-in.
    /// </summary>
    public static class AddinConstants
    {
        /// <summary>
        /// Subfolder name used for all add-in data storage (data downloads, connections, locators).
        /// </summary>
        public const string DataSubfolder = "OvertureProAddinData";
    }
}
