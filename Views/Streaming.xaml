<UserControl x:Class="DuckDBGeoparquet.Views.StreamingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"    
             xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"                       
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="283.333"
             d:DataContext="{Binding Path=DuckDBGeoparquet.Views.StreamingViewModel, Source={StaticResource Locator}}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component/Themes/Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Height="30" KeyboardNavigation.TabNavigation="Local">
            <controls:BurgerButton DockPanel.Dock="Right"
                              ToolTip="{Binding BurgerButtonTooltip}"
                              PopupMenu="{Binding BurgerButtonMenu}"/>
            <TextBlock Text="{Binding Heading}" Style="{DynamicResource Esri_TextBlockDockPaneHeader}" VerticalAlignment="Center" Margin="5,0,0,0"/>
            <Button Content="Refresh Streaming" Command="{Binding RefreshCommand}" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </DockPanel>
        <!-- Additional content can go here -->
    </Grid>
</UserControl>
